---
layout: base.njk
permalink: "/index.html"
---
<header class="hero">
  <div class="wrap">
    <div class="hero-copy">
      <p class="eyebrow">Software engineering · distributed systems · cloud</p>
      <h1>Deep dives on the systems<br>behind <span class="grad">production software</span>.</h1>
      <p class="lede">{{ site.description }} Long-form, systems-first write-ups drawn from real production work — service boundaries, consistency and failure modes, scaling bottlenecks, and the trade-offs that don't show up in the happy path.</p>
      <ul class="topics" aria-label="Topics I write about">
        <li>Distributed systems</li>
        <li>Event-driven &amp; microservices</li>
        <li>AWS &amp; Kubernetes</li>
        <li>Data &amp; ML platforms</li>
        <li>Go · Python · TypeScript</li>
      </ul>
      <div class="cta-row">
        <a class="btn btn-primary" href="/#notes">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><path d="M4 6h16M4 12h16M4 18h10"/></svg>
          Browse all posts
        </a>
        <a class="btn btn-ghost" href="/feed.xml">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><path d="M4 11a9 9 0 019 9M4 4a16 16 0 0116 16"/><circle cx="5" cy="19" r="1.4" fill="currentColor" stroke="none"/></svg>
          Subscribe via RSS
        </a>
      </div>
    </div>
    <div class="hero-illustration" aria-hidden="true">
        <svg viewBox="0 0 440 340" xmlns="http://www.w3.org/2000/svg">
          <!-- connectors -->
          <g stroke="var(--hair)" stroke-width="2" fill="none">
            <path d="M220 70 V92 M64 92 H376 M64 92 V120 M220 92 V120 M376 92 V120"/>
            <path d="M64 178 V214 M220 178 V214 M376 178 V214"/>
            <path d="M150 250 V276 M290 250 V276"/>
          </g>
          <g class="flow-group" fill="none" stroke-linecap="round">
            <path class="flow" d="M220 70 V92 M64 92 H376 M64 92 V120 M220 92 V120 M376 92 V120"/>
            <path class="flow" style="animation-delay:.45s" d="M64 178 V214 M220 178 V214 M376 178 V214"/>
            <path class="flow" style="animation-delay:.9s" d="M150 250 V276 M290 250 V276"/>
          </g>
          <g fill="var(--accent)">
            <circle cx="64" cy="92" r="3"/><circle cx="220" cy="92" r="3"/><circle cx="376" cy="92" r="3"/>
            <circle cx="150" cy="250" r="3"/><circle cx="290" cy="250" r="3"/>
          </g>

          <!-- API gateway -->
          <rect x="160" y="34" width="120" height="38" rx="10" fill="var(--surface)" stroke="var(--hair)" stroke-width="1.5"/>
          <rect x="174" y="48" width="12" height="10" rx="2.5" fill="var(--accent)"/>
          <rect x="196" y="49" width="68" height="8" rx="4" fill="var(--muted)" opacity=".5"/>

          <!-- services -->
          <g>
            <rect x="14" y="120" width="100" height="58" rx="11" fill="var(--surface)" stroke="var(--hair)" stroke-width="1.5"/>
            <rect x="28" y="134" width="14" height="14" rx="3.5" fill="var(--accent)"/>
            <rect x="50" y="137" width="46" height="7" rx="3.5" fill="var(--muted)" opacity=".5"/>
            <rect x="28" y="158" width="68" height="7" rx="3.5" fill="var(--hair)"/>
          </g>
          <g>
            <rect x="170" y="120" width="100" height="58" rx="11" fill="var(--surface)" stroke="var(--hair)" stroke-width="1.5"/>
            <rect x="184" y="134" width="14" height="14" rx="3.5" fill="var(--accent)"/>
            <rect x="206" y="137" width="46" height="7" rx="3.5" fill="var(--muted)" opacity=".5"/>
            <rect x="184" y="158" width="68" height="7" rx="3.5" fill="var(--hair)"/>
          </g>
          <g>
            <rect x="326" y="120" width="100" height="58" rx="11" fill="var(--surface)" stroke="var(--hair)" stroke-width="1.5"/>
            <rect x="340" y="134" width="14" height="14" rx="3.5" fill="var(--accent)"/>
            <rect x="362" y="137" width="46" height="7" rx="3.5" fill="var(--muted)" opacity=".5"/>
            <rect x="340" y="158" width="68" height="7" rx="3.5" fill="var(--hair)"/>
          </g>

          <!-- event bus -->
          <rect x="14" y="214" width="412" height="34" rx="9" fill="var(--bg-tint)" stroke="var(--hair)" stroke-width="1.5"/>
          <g fill="var(--accent)">
            <rect x="34" y="227" width="34" height="8" rx="4"/>
            <rect x="130" y="227" width="34" height="8" rx="4" opacity=".55"/>
            <rect x="226" y="227" width="34" height="8" rx="4"/>
            <rect x="322" y="227" width="34" height="8" rx="4" opacity=".55"/>
          </g>
          <circle cx="392" cy="231" r="6" fill="none" stroke="var(--accent)" stroke-width="2.5"/>

          <!-- datastores -->
          <g fill="var(--surface)" stroke="var(--hair)" stroke-width="1.5">
            <ellipse cx="150" cy="276" rx="34" ry="9" fill="var(--bg-tint)"/>
            <path d="M116 276 V312" fill="none"/><path d="M184 276 V312" fill="none"/>
            <path d="M116 312 a34 9 0 0 0 68 0" fill="none"/>
            <ellipse cx="290" cy="276" rx="34" ry="9" fill="var(--bg-tint)"/>
            <path d="M256 276 V312" fill="none"/><path d="M324 276 V312" fill="none"/>
            <path d="M256 312 a34 9 0 0 0 68 0" fill="none"/>
          </g>
          <g fill="var(--accent)">
            <rect x="138" y="290" width="24" height="6" rx="3"/>
            <rect x="278" y="290" width="24" height="6" rx="3"/>
          </g>
        </svg>
      </div>
  </div>
  <a class="scroll-cue" href="#notes" aria-label="Browse posts below">
    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"><path d="M6 9l6 6 6-6"/></svg>
  </a>
</header>

<section class="cred" aria-label="What this blog covers">
  <div class="wrap">
    <div class="cred-item">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M23 6l-9.5 9.5-5-5L1 18"/><path d="M17 6h6v6"/></svg>
      <div><strong>Built for scale</strong><span>Services that hold up under real production load</span></div>
    </div>
    <div class="cred-item">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/></svg>
      <div><strong>Designed to fail well</strong><span>Resilience, consistency, and graceful degradation</span></div>
    </div>
    <div class="cred-item">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M4.5 16.5L3 21l4.5-1.5M14 4s4 1 6 3 3 6 3 6l-8 8-6-6 8-8z"/><circle cx="15" cy="9" r="1.5"/></svg>
      <div><strong>Shipped, not theoretical</strong><span>Notes from systems running in production</span></div>
    </div>
  </div>
</section>

<main>
  <section class="notes wrap" id="notes">
    <div class="notes-head">
      <h2>Latest posts <span class="count" id="note-count">0</span></h2>
      <div class="controls">
        <label class="search">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><circle cx="11" cy="11" r="7"/><path d="M21 21l-4-4"/></svg>
          <input type="search" id="note-search" placeholder="Search notes…" aria-label="Search notes">
        </label>
        <div class="filter-wrap">
          <button class="ctrl-btn" id="filter-btn" aria-haspopup="true" aria-expanded="false">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><path d="M3 5h18M6 12h12M10 19h4"/></svg>
            <span id="filter-label">Filter</span>
            <span class="filter-clear" id="filter-clear" role="button" aria-label="Clear filter" hidden><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round"><path d="M6 6l12 12M18 6L6 18"/></svg></span>
          </button>
          <div class="filter-menu" id="filter-menu" role="menu"></div>
        </div>
        <div class="viewtoggle" role="group" aria-label="View">
          <button id="view-grid" aria-pressed="true" aria-label="Grid view"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="3" width="7" height="7" rx="1.5"/><rect x="14" y="3" width="7" height="7" rx="1.5"/><rect x="3" y="14" width="7" height="7" rx="1.5"/><rect x="14" y="14" width="7" height="7" rx="1.5"/></svg></button>
          <button id="view-list" aria-pressed="false" aria-label="List view"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><path d="M8 6h13M8 12h13M8 18h13M3 6h.01M3 12h.01M3 18h.01"/></svg></button>
        </div>
      </div>
    </div>

    <div class="cards" id="cards"></div>
    <div class="loadmore-row"><button class="loadmore" id="loadmore" hidden>Load more <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><path d="M12 5v14M19 12l-7 7-7-7"/></svg></button></div>
    <p class="empty" id="empty" hidden>No notes match your search.</p>
  </section>

  <script id="posts-data" type="application/json">{{ collections.posts | postsJson | safe }}</script>
  <script>
  (function () {
    var DATA = JSON.parse(document.getElementById("posts-data").textContent || "[]");
    var STEP = 6;
    var state = { q: "", cat: null, view: "grid", shown: STEP };
    try { var v = localStorage.getItem("notes-view"); if (v) state.view = v; } catch (e) {}

    var $cards = document.getElementById("cards");
    var $count = document.getElementById("note-count");
    var $empty = document.getElementById("empty");
    var $more  = document.getElementById("loadmore");
    var $search = document.getElementById("note-search");
    var $gridBtn = document.getElementById("view-grid");
    var $listBtn = document.getElementById("view-list");
    var $filterBtn = document.getElementById("filter-btn");
    var $filterMenu = document.getElementById("filter-menu");
    var $filterLabel = document.getElementById("filter-label");
    var $filterClear = document.getElementById("filter-clear");

    function esc(s){ return String(s).replace(/[&<>"]/g, function(c){ return {"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;"}[c]; }); }

    // Build filter menu from unique categories
    var cats = []; DATA.forEach(function(p){ (p.categories||[]).forEach(function(c){ if(cats.indexOf(c)<0) cats.push(c); }); });
    var menuHtml = '<button role="menuitem" data-cat="" aria-pressed="true">All categories</button>';
    cats.forEach(function(c){ menuHtml += '<button role="menuitem" data-cat="'+esc(c)+'">'+esc(c)+'</button>'; });
    $filterMenu.innerHTML = menuHtml;

    function filtered(){
      var q = state.q.toLowerCase();
      return DATA.filter(function(p){
        var inCat = !state.cat || (p.categories||[]).indexOf(state.cat) >= 0;
        var hay = (p.title+" "+p.description+" "+(p.categories||[]).join(" ")+" "+(p.keywords||[]).join(" ")).toLowerCase();
        return inCat && (!q || hay.indexOf(q) >= 0);
      });
    }

    function metaHtml(p){
      return '<span class="mi"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="4" width="18" height="17" rx="2"/><path d="M3 9h18M8 2v4M16 2v4"/></svg>'+esc(p.date)+'</span>'+
             '<span class="mi"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="9"/><path d="M12 7v5l3 2"/></svg>'+p.minutes+' min read</span>';
    }
    function thumbHtml(p){
      return '<span class="thumb"><img src="'+esc(p.thumb)+'" alt="" loading="lazy" onerror="this.parentNode.classList.add(\'fallback\');this.remove()"></span>';
    }
    function tagsHtml(p){
      var kws = (p.keywords||[]).slice(0, 3);
      if (!kws.length) return "";
      return '<ul class="card-tags">'+kws.map(function(k){ return '<li>'+esc(k)+'</li>'; }).join("")+'</ul>';
    }
    function cardHtml(p){
      var cat = (p.categories||[]).join(" · ");
      return '<article class="card"><a class="cardlink" href="'+esc(p.url)+'">'+
        thumbHtml(p)+
        '<div class="body">'+
          (cat?'<span class="pill-cat">'+esc(cat)+'</span>':'')+
          '<h3>'+esc(p.title)+'</h3>'+
          '<p class="dek">'+esc(p.description)+'</p>'+
          tagsHtml(p)+
          '<div class="meta">'+metaHtml(p)+
            '<span class="chev"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><path d="M9 6l6 6-6 6"/></svg></span>'+
          '</div>'+
        '</div>'+
      '</a></article>';
    }

    function render(){
      var list = filtered();
      $count.textContent = list.length;
      $cards.className = "cards" + (state.view === "list" ? " list" : "");
      $gridBtn.setAttribute("aria-pressed", state.view === "grid");
      $listBtn.setAttribute("aria-pressed", state.view === "list");
      var slice = list.slice(0, state.shown);
      $cards.innerHTML = slice.map(cardHtml).join("");
      $empty.hidden = list.length !== 0;
      $more.hidden = list.length <= state.shown;
      $filterBtn.classList.toggle("is-active", !!state.cat);
      $filterClear.hidden = !state.cat;
    }

    function clearFilter(){
      state.cat = null; state.shown = STEP;
      $filterLabel.textContent = "Filter";
      Array.prototype.forEach.call($filterMenu.children, function(x){ x.setAttribute("aria-pressed", x.getAttribute("data-cat") === ""); });
      render();
    }

    $search.addEventListener("input", function(){ state.q = this.value; state.shown = STEP; render(); });
    $more.addEventListener("click", function(){ state.shown += STEP; render(); });
    $gridBtn.addEventListener("click", function(){ state.view = "grid"; persistView(); render(); });
    $listBtn.addEventListener("click", function(){ state.view = "list"; persistView(); render(); });
    function persistView(){ try { localStorage.setItem("notes-view", state.view); } catch (e) {} }

    $filterBtn.addEventListener("click", function(e){
      if (e.target.closest("#filter-clear")) { e.stopPropagation(); clearFilter(); return; }
      e.stopPropagation(); var open=$filterMenu.classList.toggle("open"); $filterBtn.setAttribute("aria-expanded", open);
    });
    document.addEventListener("click", function(){ $filterMenu.classList.remove("open"); $filterBtn.setAttribute("aria-expanded","false"); });
    $filterMenu.addEventListener("click", function(e){
      var b = e.target.closest("button"); if(!b) return;
      state.cat = b.getAttribute("data-cat") || null; state.shown = STEP;
      Array.prototype.forEach.call($filterMenu.children, function(x){ x.setAttribute("aria-pressed", x===b); });
      $filterLabel.textContent = state.cat || "Filter";
      render();
    });

    render();
  })();
  </script>
</main>
